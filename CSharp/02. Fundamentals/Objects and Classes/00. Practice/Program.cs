using System;
using System.Globalization;

namespace _00._Practice
{
    class Program
    {
        static void Main(string[] args)
        {

            //Cat cat = new Cat();
            //cat.Name = "Sara";


            // Anonymous class var birthday = new { Day = 26, Month = 10, Year = 1990 };
            // Console.WriteLine(birthday.Day) ;
            // Console.WriteLine(cat.Name);

            //string input = "02-19-2020";
            //DateTime dateTime = DateTime.ParseExact(input, "MM-dd-yyyy", CultureInfo.InvariantCulture);
            //dateTime = dateTime.AddDays(2);
            //Console.WriteLine(dateTime.DayOfWeek);

            //Console.WriteLine(DateTime.Now.ToString("dddd/dd/MMMM/yyyy"));
            //#################################################################
            //18-04-2016
            //string dateAsString = Console.ReadLine();
            //DateTime dateTime = DateTime.ParseExact(dateAsString, "dd-MM-yyyy", CultureInfo.InvariantCulture);
            //Console.WriteLine(dateTime.DayOfWeek);

            //Random random = new Random();
            //int result = random.Next(0, 18);
            //Console.WriteLine(result);


        }
    }
}
